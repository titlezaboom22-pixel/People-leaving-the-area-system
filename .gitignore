node_modules
dist
.DS_Store
*.log
.env
.env.local
.firebase
*.local

# IDE / Editor
.vscode
.idea
.claude

# OS
Thumbs.db

# PWA APK signing keys (ห้าม commit!)
*.keystore
signing-key-info.txt
tmp_pwa_zip/
tmp_ios_zip/

# Firebase Admin SDK service account (ห้าม commit!)
server/firebase-admin-key.json
firebase-admin-key.json
*-firebase-adminsdk-*.json

# Firestore backup files (มีข้อมูลผู้ใช้ — ห้าม commit!)
backups/
backup-*.json

# Backups (มีข้อมูลพนักงาน — ห้าม commit)
backups/

# Docker env (มี API keys + SMTP)
.env.docker
.env.bak
